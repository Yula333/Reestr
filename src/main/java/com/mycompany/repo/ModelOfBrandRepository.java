package com.mycompany.repo;

import com.mycompany.models.ModelOfBrand;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ModelOfBrandRepository extends JpaRepository<ModelOfBrand, Integer> {
}
